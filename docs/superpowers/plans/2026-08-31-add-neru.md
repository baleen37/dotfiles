# Neru Installation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restore Neru as a reproducible macOS Homebrew/Home Manager feature with Neru's default `Primary+Shift+Space` hints shortcut.

**Architecture:** Homebrew owns the Neru application and its tap declaration. Home Manager owns a version-controlled `~/.config/neru/config.toml` with Neru's default hints shortcut. Existing Karabiner and Hammerspoon behavior remains unchanged.

**Tech Stack:** Nix flakes, nix-darwin/Home Manager, Homebrew casks, Karabiner JSON generated by Nix, Neru TOML configuration.

**Spec:** User request `neru를 깔아줄래?`, interpreted against the previously removed implementation at `4fee159f^` in this repository.

## Global Constraints

- Keep the scope to Neru installation/configuration and its default hints binding.
- Preserve the current worktree's unrelated files and the current Hammerspoon window-switcher implementation.
- Keep the existing platform defaults: Neru and Karabiner are enabled only on Darwin.
- Verify the declarative path with buildable Nix assertions before activation.
- Verify the installed Neru binary with `neru --version`, `neru config validate`, and `neru status` after activation.

---

### Task 1: Add failing coverage for Neru declarations

**Files:**

- Modify: `tests/unit/modules-namespace-test.nix`
- Modify: `tests/integration/home-manager-test.nix`
- Modify: `tests/integration/karabiner-test.nix`
- Modify: `tests/unit/darwin-test.nix`
- Modify: `tests/unit/platform-defaults-test.nix`
- Create: `tests/integration/neru-test.nix`

**Interfaces:**

- Consumes: Existing module namespace, Home Manager import, and Karabiner JSON test patterns.
- Produces: Assertions requiring `modules.programs.neru.enable`, the Home Manager import, a forced Neru config file, and Neru's default hints shortcut.

- [ ] Add `neru` to the program module namespace, Darwin platform-default, and Homebrew cask/tap tests, add `programs/neru.nix` to the Home Manager import list, and keep the expected Karabiner local keys at `[ "b" "u" ]`.
- [ ] Add `tests/integration/neru-test.nix` that imports `users/shared/programs/neru.nix`, asserts the `home.file.".config/neru/config.toml"` entry exists with `force = true`, and asserts the source TOML contains Neru's default hints hotkey, enabled hints/grid sections, and scroll settings.
- [ ] Run the focused Darwin checks and confirm they fail because the Neru module/config and binding are not yet present.

### Task 2: Implement the minimal declarative Neru feature

**Files:**

- Create: `users/shared/programs/neru.nix`
- Create: `users/shared/programs/.config/neru/config.toml`
- Modify: `users/shared/home-manager.nix`
- Modify: `users/shared/darwin/homebrew.nix`
- Modify: `users/shared/programs/karabiner.nix`

**Interfaces:**

- Consumes: The failing assertions from Task 1 and the last known-good Neru configuration at `4fee159f^`.
- Produces: Darwin-gated `modules.programs.neru.enable`, a forced Home Manager config link, the `y3owk1n/tap` tap/cask, and Neru's default hints shortcut.

- [ ] Restore the small program module pattern used by the existing GUI modules, with `lib.mkEnableOption "Neru (macOS)"` defaulting to `pkgs.stdenv.hostPlatform.isDarwin` and `home.file.".config/neru/config.toml"` sourced from the repository with `force = true`.
- [ ] Restore the Neru TOML configuration from `4fee159f^`, retaining its navigation-mode settings, set hints to Neru's default `Primary+Shift+Space` shortcut, and update its accessibility role names to the current semantic vocabulary accepted by the installed Neru CLI.
- [ ] Add `"y3owk1n/tap/neru"` to the Homebrew cask list and `"y3owk1n/tap"` to the taps list.
- [ ] Add `./programs/neru.nix` to `users/shared/home-manager.nix` imports without force-enabling the Darwin-gated module.
- [ ] Leave the existing Karabiner local bindings unchanged so Neru owns its default shortcut directly.

### Task 3: Verify source and generated configuration

**Files:**

- No further source changes unless a focused check exposes an implementation error.

- [ ] Run `nix build '.#checks.aarch64-darwin.integration-neru' --accept-flake-config --print-build-logs --no-link` and the focused Karabiner/module/Home Manager checks.
- [ ] Run `nix fmt -- --fail-on-change` and `git diff --check`.
- [ ] Run `make test-build` so every unit/integration assertion is actually built on this Darwin host.
- [ ] Run `neru --version`, `neru config validate`, and inspect `neru status --json` without printing secrets.

### Task 4: Apply the installed configuration

**Files:**

- No further source changes.

- [ ] Confirm the Homebrew cask is already installed or reconcile it through the repository's full `make switch` path.
- [ ] Run `make switch-home HM_USER=jito.hello` to apply the Home Manager config and Karabiner JSON.
- [ ] Verify `~/.config/neru/config.toml` is the Home Manager-managed file, Neru remains callable, and the daemon reports the config path and enabled modes.
- [ ] Report separately what is declaratively implemented, what was activated, and what GUI/key-event behavior could or could not be exercised in this session.
